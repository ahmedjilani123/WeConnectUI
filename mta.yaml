_schema-version: "3.2"
ID: weconnectg
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: weconnectg-destination-content
  type: com.sap.application.content
  requires:
  - name: weconnectg-destination-service
    parameters:
      content-target: true
  - name: weconnectg-repo-host
    parameters:
      service-key:
        name: weconnectg-repo-host-key
  - name: weconnectg-uaa
    parameters:
      service-key:
        name: weconnectg-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: weconnectg_html_repo_host
          ServiceInstanceName: weconnectg-html5-service
          ServiceKeyName: weconnectg-repo-host-key
          sap.cloud.service: weconnectg
        - Authentication: OAuth2UserTokenExchange
          Name: weconnectg_uaa
          ServiceInstanceName: weconnectg-xsuaa-service
          ServiceKeyName: weconnectg-uaa-key
          sap.cloud.service: weconnectg
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: weconnectg-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: weconnectg-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - weconnectg.zip
      name: weconnectg
      target-path: resources/
- name: weconnectg
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: weconnectg-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: weconnectg-destination-service
    service-plan: lite
- name: weconnectg-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: weconnectg-xsuaa-service
    service-plan: application
- name: weconnectg-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: weconnectg-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
